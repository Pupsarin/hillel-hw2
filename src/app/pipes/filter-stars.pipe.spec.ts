import { FilterStarsPipe } from './filter-stars.pipe';

describe('FilterStarsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStarsPipe();
    expect(pipe).toBeTruthy();
  });
});
